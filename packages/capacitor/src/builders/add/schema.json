{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://json-schema.org/draft-07/schema",
  "title": "Add builder",
  "description": "Runs a builder that adds a new native platform to a Capacitor project.",
  "type": "object",
  "properties": {
    "platform": {
      "description": "The native platform to add.",
      "type": "string",
      "enum": ["android", "ios"]
    }
  },
  "required": []
}
