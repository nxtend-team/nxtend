import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: '<%= appId %>',
  appName: '<%= appName %>',
  webDir: "<%= pathToRoot %>/<%= webDir %>",
  bundledWebRuntime: false
}

export default config;
