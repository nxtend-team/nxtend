{
  "$schema": "http://json-schema.org/schema",
  "$id": "FirebaseProject",
  "title": "Create a Firebase project for an Nx application",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the frontend project for Firebase.",
      "alias": "p",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "What is the name of the frontend project for Firebase?"
    }
  },
  "required": ["project"]
}
