{
  "$schema": "http://json-schema.org/schema",
  "title": "Firebase builder",
  "description": "Run a Firebase command.",
  "type": "object",
  "properties": {
    "cmd": {
      "description": "The Firebase command.",
      "type": "string"
    }
  },
  "required": []
}
