{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Firebase executor",
  "description": "Execute a Firebase command",
  "type": "object",
  "properties": {
    "cmd": {
      "description": "The Firebase command to execute.",
      "type": "string"
    }
  },
  "required": []
}
