{
  "$schema": "http://json-schema.org/draft-07/schema",
  "id": "Post",
  "title": "Create a dev.to Blog Post",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to add a blog to.",
      "alias": "p",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "What is the name of the project to add a blog to?"
    },
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "directory": {
      "type": "string",
      "description": "A directory where the blog post is placed.",
      "alias": "d"
    },
    "id": {
      "type": "string",
      "description": "The ID of the blog post.",
      "x-prompt": "What is the ID of the blog post?"
    }
  },
  "required": ["project", "name", "id"]
}
